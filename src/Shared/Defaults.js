/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class Vi_Bwl_Shared_Defaults {

    NAME = '@flancer64/gpt-vi-bwl';

    constructor() {
        Object.freeze(this);
    }
}
